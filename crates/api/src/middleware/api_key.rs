// Legacy API key middleware removed — authentication is now handled
// by session-based auth (require_auth middleware) and API token validation
// (ValidateApiTokenUseCase in the application layer).
