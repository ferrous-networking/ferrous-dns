# PowerDNS Recursor — Ferrous-DNS benchmark config
# Fair comparison: same upstreams, cache enabled, log info, rate limiting disabled

local-address=0.0.0.0
local-port=53
forward-zones-recurse=.=8.8.8.8;1.1.1.1
threads=16
max-cache-entries=200000
ratelimit=0
loglevel=5
